<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dingjiangying.webmonitor.dao.UserMapper">

    <resultMap id="userMap" type="UserPo">
        <id property="UserId" column="user_id"/>
        <result property="UserName" column="user_name"/>
        <result property="UserPassword" column="user_password"/>
    </resultMap>

    <select id="getUsers" resultMap="userMap">
        select * from user;
    </select>

    <select id="getUser" resultMap="userMap" parameterType="String">
        select * from user where user_id = #{id};
    </select>

    <insert id="insertUser" useGeneratedKeys="true" keyColumn="userId"
            keyProperty="userId" parameterType="com.dingjiangying.webmonitor.po.UserPo">
        insert into user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="po.userId != null">
                user_id,
            </if>
            <if test="po.userName != null">
                user_name,
            </if>
            <if test="po.userPassword != null">
                user_password,
            </if>
        </trim>
        values
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="po.userId != null">
                #{po.userId},
            </if>
            <if test="po.userName != null">
                #{po.userName},
            </if>
            <if test="po.userPassword != null">
                #{po.userPassword},
            </if>
        </trim>
    </insert>

</mapper>