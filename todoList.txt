重要性依次递减：
0. "查看脚本输出"过慢,以及下载找不到位置的问题,以及返回的时候把下到本地的文件删了
1. 试着跑一遍，保证主流程不出bug
2. 什么"LogList"之类的大标题都改为中文，风格统一，不要出现英文
3. loglist的"执行状态码"改为1为红色"告警"方块，0为绿色"正常"方块，2为"已读"蓝色方块，为null的暂不显示吧
4. 其他 - 时间类 - 状态（带色方块）- 操作 - 日志  所有列表按这个顺序从左到右显示
5. 探针管理的探针状态先写死为"运行中"，绿色方块
6. 监控大盘加上"总任务数"、"未读告警"（几条告警状态的日志就几条），点进未读告警跳转到loglist，
不过只显示告警的log，loglist后面加个操作"已读"（只有告警状态的log有这个操作），点击后状态变成"已读"（一共三种状态）
7. 最好做个镜像，from centos之类，yum install java;node; npm install puppeteer;puppeteer-har，然后项目打jar包放进去，
尝试在服务器上用这个image成功跑起来。
8. 7做到后可以添加探针功能了，给一个虚拟的ssh-key（之后我会换成我电脑的），文字指导用户放到自己的服务器的.ssh/authrization...里面，
然后用户输入主机用户名和ip，创建探针。同时服务器被主服务器用ssh安装了docker，复制过去了image，启动起来jar包了，就开始拉任务执行任务了


0.删除没做 太慢就是scp太慢 想办法可以直接从源阿里服务器下载  不用先下载到本地
